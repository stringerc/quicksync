- Opened Lawbot UI in browser (`http://127.0.0.1:8765/`). Documented Lawbot in `~/MEMORY.md` and added `lawbot/MEMORY.md` quick reference.
- **Stale uvicorn**: If `/` and `/static/*` return 404 but `/health` works, restart the server so the process loads the current app (UI routes + static). Verified: GET `/` 200, static assets 200, POST `/v1/chat` OK, `tests.test_ui_audit` pass.
- **Gwinnett Magistrate template:** Added `lawbot/gwinnett_magistrate.py` — injects official MAG 10-xx / forms URL / clerk phone into the prompt when the message + profile look like Gwinnett civil/magistrate work and document mode, strengthen, or citation-audit is on. Updated `document_review.py` section hints for Gwinnett-specific forms.
- **UI:** Document mode is always on from the web UI (no checkbox). Assistant replies render as Markdown in the browser (marked + DOMPurify); per-reply **Download** saves `.md`. Sidebar **Export .md** still exports the whole chat.
- **Roadmap:** Added canonical exhaustive program doc `docs/LAWBOT_PARITY_ROADMAP.md` — resource audit, phased roadmap (0–8), multi-provider strategy, measurement definition, test/script appendices, cost governance.
- **Implemented Phases 1–2:** `decide_llm_route` + `LAWBOT_ANTHROPIC_ESCALATION` / daily cap / `ANTHROPIC_ESCALATION_MODEL`; structured JSON logs (`lawbot/observability.py`); per-turn `llm_backend` in responses; `llm_daily_counts` table; tests `test_llm_route`, `test_usage_limits`; `/health` exposes escalation fields; `docs/ARCHITECTURE.md` Anthropic row updated.
- **Hardening:** `configure_llm_logging()` on app startup so `lawbot.llm` JSON lines aren’t dropped by uvicorn’s WARNING root; `scripts/verify_apis.py` probes Anthropic whenever key present (dual-backend); `draft_judge` supports Anthropic-only installs; token usage in logs only if `int` (avoids mock leakage); `tests/test_draft_judge.py`; full unittest 148 OK.
- **Phases 3–8 (implemented in repo):** `reasoning_depth` + optional Anthropic extended thinking; `session_compaction` → `SESSION MEMORY`; `retrieval_query` augment + dedupe; Hermes placeholder chunk check; mobile CSS; `rate_limit` middleware; `docs/OPERATIONS.md`; roadmap updated; unittest 158 OK.
- **NVIDIA-only max IQ:** `LAWBOT_AUXILIARY_MODEL_TIER` + `settings.auxiliary_chat_model_id()` — reasoning/compaction/draft judge default to **primary** (`CHAT_MODEL`); `docs/NVIDIA_MAX_QUALITY.md`; `verify_apis.py` prints auxiliary model.
- **Developer workflow (no runtime cost):** Added `CLAUDE.md`, `docs/DEVELOPER_WORKFLOW.md`, `scripts/security_scan.sh` (ruff + bandit medium+), optional `.pre-commit-config.yaml`, CI runs security scan; `requirements-dev.txt` includes bandit; README links; removed unused `doc_mode_early` in `chat_turn.py` (Ruff F841).
- **Cursor:** `.cursor/rules/lawbot-core.mdc` + `lawbot-python.mdc`, `.vscode/settings.json` for `.venv`, CLAUDE.md + DEVELOPER_WORKFLOW.md updated for IDE usage.
- **Dev tooling follow-up:** Pinned `ruff==0.15.9` / `bandit==1.9.4` in `requirements-dev.txt` + CI; `security_scan.sh` lints `scripts/`; `scripts/pre_commit_security.sh` + monorepo-aware `.pre-commit-config.yaml`; `lawbot-static.mdc` for static UI; DEVELOPER_WORKFLOW Cursor Rules + monorepo note.
- **SQLite / strict tests:** Lifespan now closes any pre-existing `app.state.conn`, uses `try/finally` to always close on shutdown (no parallel `TestClient` threads — that orphaned sqlite connections). Added `test_large_user_message_does_not_500` with mocked LLM. `bash scripts/strict_test.sh` 207 OK, clean output (no ResourceWarning flood).
- **Concurrent health load test:** `TestConcurrentHealth` uses `asgi-lifespan` `LifespanManager` + `httpx.AsyncClient` + `asyncio.gather` (48 parallel GET `/health`, single lifespan). Dependency: `asgi-lifespan>=2.1.0` in `requirements.txt`.
