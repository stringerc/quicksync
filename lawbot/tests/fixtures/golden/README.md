# Golden draft fixtures

Synthetic redacted texts — **not** real filings. `manifest.json` lists cases; `tests/test_golden_drafts.py` asserts weighted `draft_quality` scores.

Run locally:

```bash
bash scripts/ci_quality.sh
```
